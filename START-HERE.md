# 从这里开始

> 给用户的 3 步开始指引。技术细节见 [README.md](./README.md)，Agent 入口见 [AGENTS.md](./AGENTS.md)。

## 现在的状态（已装好）

| 项 | 状态 |
|---|---|
| 全局命令 `ai-growth` | ✅ 已就绪（终端可直接用） |
| 数据库 | ✅ `~/.ai-growth/growth.db`（空，等访谈） |
| 常驻服务 daemon | ✅ 由 launchd 托管，开机自启（面板 + 定时提醒） |
| 面板 | ✅ http://127.0.0.1:4580 |
| Skill（告诉 Agent 什么时候用它） | ✅ 已装到 `~/.workbuddy/skills/ai-growth/` |
| MCP（63 个工具） | ✅ 已写入 `~/.workbuddy/mcp.json` |

## 你需要做的 3 步

### 第 1 步：在 Agent 里「信任」这个 MCP

打开 WorkBuddy 的**连接器管理页 → 右上角「自定义连接器」→ 找到 `ai-growth` → 点「信任」**。

> 这是唯一还需你手点的一步（配置已写好，但出于安全 MCP 不会自动激活）。

### 第 2 步：开一个新对话

Skill 和 MCP 是**会话开始时**加载的，所以已经开着的对话看不到它。新开一个对话。

### 第 3 步：说一句启动语

```
开启今日任务
```

同义说法（任选，效果一样）：

```
今天要做什么
开始今天的学习
今天安排什么
开工
```

Agent 会调用 `growth_status`（等价于 CLI `ai-growth start`），然后**按状态决定**：

| 你的状态 | Agent 会做 |
|---|---|
| 还没访谈过（现在就是这样） | **先访谈，不会直接派任务** |
| 有未处理的重排主线 | 先解决它，再谈今天 |
| 正常 | 给出今天的 1 个主任务 + 0~2 个每日任务 |

首次访谈它会问这些（用真实情况回答即可，不用准备）：

- 你现在主要想做成什么？希望最后达到什么程度（不是"学完什么"）
- 想多久做到？每天大概能投入多少时间
- 现在的基础到哪了
- 生活上有什么约束（作息、工作、家庭）
- 想改善哪些生活领域（运动 / 睡眠 / 关系 / 其他）
- 每天能接受几个任务

回答完它会给你一份 **Goal 草案 + 阶段计划**，你确认后才开始生成今天的任务。

> ⚠️ 「开启今日任务」不是"立刻给我一堆任务"。第一次它会先访谈——这是刻意的（AI 管路线，人决定目的地）。

---

## 怎么让 Agent 一直用这套系统

Skill 的 `description` 已经写明触发条件，所以**相关话题会自动走 AI Growth**。你不需要每次都点名。

**会自动触发的说法：**

| 你说 | Agent 会做 |
|---|---|
| **开启今日任务** / 今天要做什么 / 开工 | 跨天检查 → 给出今日计划（首次会先访谈） |
| 开始今天的学习 | 同上（学习型目标的常用说法） |
| 我想 30 天学会 XXX | 走访谈 → 建 Goal → 分阶段 |
| 这个任务我不想做 / 换一个 | 记为反馈并替换（不是简单删除） |
| 我这周特别忙 / 明天只有半小时 | 调整负荷（LifeContext） |
| 我最近进度怎么样 / 帮我复盘 | 汇总证据与成长记录 |
| 我做完了 / 搞定了 | 完成任务并记录证据 |
| 昨天那个没做完 | 走 Replan（不作废、不累积） |

**想强制指名，也可以直接说：**

```
用 ai-growth 看看我现在该做什么
帮我用 ai-growth 复盘这周
```

**如果 Agent 没走系统**（比如自己给你列了个 todo 列表），直接纠正它：

```
用 ai-growth 来做，不要自己列清单
```

## 随时自查（终端里）

```bash
ai-growth start            # 开启今日任务（跨天检查 + 状态 + 今日计划 + 该做什么）
ai-growth status           # 只看状态 + 建议下一步
ai-growth today            # 只看今日计划
ai-growth where            # 跑的是哪一份代码、数据在哪、怎么更新
ai-growth help             # 全部命令
```

Agent 也能自己查（这就是"Agent 知道自己该怎么用"的机制）：

```bash
ai-growth agent-info --json    # 安装位置、数据目录、MCP 配置、Skill 位置、下一步
```

## 日常使用节奏（建议）

| 时机 | 做什么 |
|---|---|
| 早上/开始工作前 | 对 Agent 说「开启今日任务」→ 看它给的 1 主 + 0~2 每日任务 |
| 觉得不合适 | 直接说「这个不合理 / 太难 / 没时间」→ 它会换，不会硬推 |
| 做完了 | 说一声「做完了」，Agent 会记证据 |
| 想看进度 | 说「给我看看进度」→ Agent 会给出**图表式报告**（不是流水账） |
| 睡前（可选） | 说「复盘一下」 |
| 每周（可选） | 说「这周复盘」→ 看阶段进度是否要调整 |

**不用做的事**：不用自己维护清单、不用手抄进度、不用管昨天没做完的——系统会自动作废每日任务、
把主任务转入待重排，不会攒成一堆欠债。

## 进度是图表，不是流水账

想让 Agent 展示进度时，它默认输出**带样式的报告**（不是一堆文字）：

- 阶段进度条 + 节点轨道（`■ ◧ □ □ □`）
- 甘特图（Mermaid，支持渲染的查看器里是可视化时间线）
- 今日任务表、事实统计表、最近进展表

你自己也可以随时在终端生成：

```bash
ai-growth progress                # 在终端直接看 Markdown
ai-growth progress --out p.md     # 存成 Markdown
ai-growth progress --out p.html   # 存成网页（双击就能打开，深色面板样式）
```

> 报告里的进度条比例来自「阶段序号 / 总数」，数字全部是数据库里的可核对事实。
> 没有"掌握度 72%"这种东西——那是编不出来的。

## 出问题怎么办

```bash
ai-growth doctor      # 逐项自检，并给出修复命令
```

| 现象 | 处理 |
|---|---|
| Agent 说没连上 ai-growth | 检查第 1 步的「信任」是否点了；再跑 `ai-growth doctor` |
| Agent 不知道要做什么 | 新开一个对话（Skill 元数据在会话开始时加载） |
| 面板打不开 | daemon 应自带面板；`ai-growth serve` 可前台跑起来 |
| 想彻底卸载 | `./scripts/uninstall.sh`（默认保留数据）；加 `--purge` 才删数据 |
